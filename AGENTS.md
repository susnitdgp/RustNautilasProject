# Project requirements

- Use Redis for all order persistence and application-state persistence. Do not introduce SQLite or another state database.
- Keep each component in a separate module.
- Keep live order submission disabled unless explicitly authorized for a later verification stage.
